import React from "react";

const Product = (props) => {
    const { img, description, price, quantity, total } = props;

    return (
        <div>
            Soy el componente de poduct
        </div>
    );
};

export default Product;